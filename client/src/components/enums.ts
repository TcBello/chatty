export enum ChattingState {
  idle = "idle",
  searching = "searching",
  chatting = "chatting",
}

export enum MessageType {
  text = "text",
  image = "image",
}

export enum DispatchStatus {
  fulfilled = "fulfilled",
  rejected = "rejected",
}
